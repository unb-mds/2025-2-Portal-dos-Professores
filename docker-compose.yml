services:

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - ./data:/app/public/data 
      - /app/node_modules

  data-miner:
    build: ./scripts
    volumes:
      - ./scripts:/app
      - ./data:/app/data